const GameBoard = (function() {
    
})();

const GamePlayer = (name, symbol) => {
    
    if (symbol === "circle"){
        const marker = "O";
    }
    else{
        const marker = "X";
    }
    return { name, marker }; 
};


